{
  "name": "github-explorer",
  "version": "3.0.0",
  "description": "GitHub Trending 项目发现 — 离线优先架构，GitHub Actions 定时抓取 + AI 翻译",
  "private": true,
  "scripts": {
    "check": "node --check server.js && node --check scripts/scrape.js && node --check scripts/scrape-safe.js",
    "scrape": "node scripts/scrape-safe.js",
    "scrape:raw": "node scripts/scrape.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
